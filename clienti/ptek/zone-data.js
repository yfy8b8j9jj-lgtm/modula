/* ─── DATI ZONE — specifici per ogni azienda (TEMPLATE VUOTO) ───
   Questo file conteneva i paesi/zone reali di un'azienda; qui è svuotato:
   la STRUTTURA (modulo zone.js) resta, i DATI no.
   Formato di ogni voce:
     { p:"NomePaese", z:"Zona", c:"CAP", la:latitudine, lo:longitudine }
   Esempio:
     { p:"Esempio", z:"A", c:"00000", la:45.0, lo:9.0 }
*/
const ZONE_PAESI=[];
/* Centro mappa di default (l'azienda lo sposta sul suo territorio). Default: centro Italia. */
const ZONE_CENTER=[42.5,12.5];
